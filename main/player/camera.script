function init(self)
	self.camera_pos = go.get_position()
end

function update(self, dt)
	local player_pos = go.get_position("player")
	local target_position = vmath.vector3(player_pos.x, player_pos.y, self.camera_pos.z)

	go.set_position(target_position)
end
